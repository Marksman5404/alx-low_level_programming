#include "main.h"
/**
 * add - function that adds two integers and returns the result.
 * @a: the first integer
 * @b: the second integer
 * Return: one and two
 */
int add(int a, int b)
{
return (a + b);
}
