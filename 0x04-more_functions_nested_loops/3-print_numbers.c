#include "main.h"

/**
 * mul - function that adds two integers and returns the result.
 * @a: the first integer
 * @b: the second integer
 * Return: one and two
 */

int mul(int a, int b);
  
{
  
  return (a + b);
  
}
