#include <stdio.h>
#include <time.h>
#include <stdlib.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int ch;

for (ch = 'A'; ch <= 'Z'; ch++)
putchar (ch);
putchar ('\n');

return (0);

}
